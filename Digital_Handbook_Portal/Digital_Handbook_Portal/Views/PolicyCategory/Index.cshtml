@model IEnumerable<Digital_Handbook_Portal.Models.PolicyCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.categoryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.subCategory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Policies)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.categoryName)
            </td>
            <td>
                @Html.DisplayFor(model => item.subCategory)
            </td>
            <td>
                @Html.DisplayFor(model => item.Policies)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.categoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.categoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.categoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
